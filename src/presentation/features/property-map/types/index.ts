/**
 * 🎨 PRESENTATION - Tipos del Mapa
 * 
 * Re-exporta los tipos genéricos del Core para uso en la capa de presentación.
 * NO duplica tipos, solo los re-exporta y añade helpers específicos de UI.
 */

export type {
  MapItem,
  MapSearchResult,
  MapCoverageType,
  MapFilters,
  MapCoverageInfo,
} from '@/core/domain/entities/MapTypes';
